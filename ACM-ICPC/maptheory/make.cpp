#include <bits/stdc++.h>
#include <windows.h>
#include <ctime>
using namespace std;
inline int random(int x)
{
    return rand()%x+1;
}
int main()
{
    srand(time(0));
    int n,m;
    n=rand(10),m=rand(10);
    return 0;
}